public class Dog {
    public String toString() {
        return super.toString() + "Woof, woof.";
    } // overwriting means I am overwriting the original, customizing it
    // super.toString accesses the class above, toString that is built as default in the Object class
}