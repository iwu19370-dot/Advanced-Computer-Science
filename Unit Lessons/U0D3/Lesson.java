public class Lesson {

    public static void main{String[] args} {
        //variables
        int number = 5; // assignment operator

        // sinfle = assigns a value
        // different from ==, which compares a value

        // dofferent assignment operators
        number += 6; // same as number = number + 6
        number -= 3; // number = number - 3
        number *= 10; // number = number * 10
        number /= 2; // number = number / 2 

        // increment and decrement operators
        number += 1; 
        number ++; // it does the same thing as the one above

        number -=1;
        number --; // same thing as -= 1

        //boolean operators, evaluate to truw or false
        // use them in if statements

        // CANNOT USE FOR OBJECTS

        /*
        number == 5; // equals
        number != 10; // not equal
        number <= 15; // less than or equal
        number >= 20; // greater than or equal
        */
        
    }
}